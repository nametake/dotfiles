[user]
  name = nametake
  email = nametake.kyarabuki@gmail.com

[push]
  default = simple
  path = ~/.gitconfig.local

[diff]
  compactionHeuristic = true

[fetch]
  prune = true

[alias]
  tree = log --graph --all --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative 
  delete-merged-branch = "!f () { git checkout $1; git branch --merged|egrep -v '\\*|develop|master'|xargs git branch -d; };f"
  ac = !git add -A && git commit
  ch = checkout
  di = diff
  p = !git push origin `git rev-parse --abbrev-ref HEAD`
  default = "!f () { git checkout master; git pull; git delete-merged-branch; };f"
  pull-master = "!f () { git checkout master; git pull; git delete-merged-branch; };f"

[core]
  editor = "vim"
  excludesfile = ~/.gitignore

[include]
  path = ~/.gitconfig.local

[ghq]
  root = ~/Workspace
  root = ~/go/src

[init]
  templatedir=~/.git_template

[url "git@github.com:"]
  insteadOf = https://github.com/
